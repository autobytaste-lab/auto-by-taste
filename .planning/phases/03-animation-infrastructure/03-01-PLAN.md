---
phase: 03-animation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/data/types.ts
  - components/data/chips.ts
  - components/data/__tests__/chips.test.ts
  - components/hooks/useReducedMotion.ts
  - components/hooks/useReducedMotion.test.ts
  - components/hooks/useInView.ts
  - components/hooks/useInView.test.ts
  - components/hooks/useCountUp.ts
  - components/hooks/useCountUp.test.ts
autonomous: true
requirements:
  - CHIP-05
  - A11Y-01
  - A11Y-04

must_haves:
  truths:
    - "M4 chips have a tops field with value 38 in the data layer"
    - "useReducedMotion returns true when prefers-reduced-motion is active and updates reactively"
    - "useInView returns false initially, true after element enters viewport, and disconnects observer"
    - "useCountUp animates from 0 to target value with ease-out timing, respects trigger parameter"
  artifacts:
    - path: "components/data/types.ts"
      provides: "Chip interface with optional tops field"
      contains: "tops?: number"
    - path: "components/data/chips.ts"
      provides: "M4 chip entries with tops: 38"
      contains: "tops: 38"
    - path: "components/hooks/useReducedMotion.ts"
      provides: "Reactive reduced-motion detection hook"
      exports: ["useReducedMotion"]
    - path: "components/hooks/useInView.ts"
      provides: "One-shot IntersectionObserver hook"
      exports: ["useInView"]
    - path: "components/hooks/useCountUp.ts"
      provides: "requestAnimationFrame count-up animation hook"
      exports: ["useCountUp"]
  key_links:
    - from: "components/hooks/useReducedMotion.ts"
      to: "window.matchMedia"
      via: "matchMedia('(prefers-reduced-motion: reduce)') with change listener"
      pattern: "addEventListener.*change"
    - from: "components/hooks/useInView.ts"
      to: "IntersectionObserver"
      via: "observer.observe with one-shot disconnect"
      pattern: "observer\\.disconnect"
    - from: "components/hooks/useCountUp.ts"
      to: "requestAnimationFrame"
      via: "rAF loop with cancelAnimationFrame cleanup"
      pattern: "cancelAnimationFrame"
---

<objective>
Add TOPS data to M4 chips and create three custom React hooks (useReducedMotion, useInView, useCountUp) that Phase 4's SVG chip diagram will consume.

Purpose: These are the animation primitives and data prerequisites. Phase 4 cannot build animated spec labels without useCountUp, cannot trigger on scroll without useInView, and cannot respect accessibility without useReducedMotion.
Output: 3 tested hooks in components/hooks/, updated chip data with TOPS field
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-animation-infrastructure/03-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From components/data/types.ts:
```typescript
export type ChipGeneration = 'M1' | 'M2' | 'M3' | 'M4';
export type ChipVariant = 'base' | 'Pro' | 'Max' | 'Ultra';

export interface Chip {
  readonly id: string;
  readonly name: string;
  readonly generation: ChipGeneration;
  readonly variant: ChipVariant;
  readonly cpuCores: { readonly performance: number; readonly efficiency: number; readonly total: number; };
  readonly gpuCores: number;
  readonly neuralEngineCores: number;
  readonly memoryBandwidth: number;
  readonly maxMemory: number;
  readonly processNode: string;
}
```

From test/setup.ts (existing matchMedia mock):
```typescript
// jsdom doesn't implement matchMedia - provide a default mock
Object.defineProperty(window, 'matchMedia', {
  writable: true,
  value: (query: string) => ({
    matches: false,
    media: query,
    onchange: null,
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => false,
    addListener: () => {},
    removeListener: () => {},
  }),
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TOPS data field + useReducedMotion hook</name>
  <files>components/data/types.ts, components/data/chips.ts, components/data/__tests__/chips.test.ts, components/hooks/useReducedMotion.ts, components/hooks/useReducedMotion.test.ts</files>
  <behavior>
    - TOPS data: M4 base/Pro/Max chips each have tops: 38; non-M4 chips have no tops field; existing chip tests still pass
    - useReducedMotion: returns false when matchMedia matches is false
    - useReducedMotion: returns true when matchMedia matches is true (override global mock per-test)
    - useReducedMotion: updates reactively when user toggles OS setting (simulate via change event listener)
    - useReducedMotion: cleans up event listener on unmount
  </behavior>
  <action>
    **TOPS data (CHIP-05):**
    1. Add `readonly tops?: number;` to the `Chip` interface in `components/data/types.ts` (optional field -- only M4 has marketed TOPS values)
    2. Add `tops: 38` to the three M4 entries (m4-base, m4-pro, m4-max) in `components/data/chips.ts`
    3. Add test assertions to `components/data/__tests__/chips.test.ts`: all M4 chips have tops === 38, non-M4 chips have tops === undefined

    **useReducedMotion hook (A11Y-01):**
    4. Create `components/hooks/useReducedMotion.ts` following the pattern from 03-RESEARCH.md Pattern 3:
       - Initialize state from `window.matchMedia('(prefers-reduced-motion: reduce)').matches`
       - Add `addEventListener('change', handler)` in useEffect for live updates
       - Clean up with `removeEventListener` on unmount
       - Handle SSR with `typeof window === 'undefined'` guard returning false
    5. Create `components/hooks/useReducedMotion.test.ts`:
       - Use a helper function `mockReducedMotion(matches: boolean)` that overrides `window.matchMedia` with a mock returning the given value and capturing listeners for simulation
       - Test: returns false by default (matches: false)
       - Test: returns true when reduced motion active (matches: true)
       - Test: updates when OS setting changes (trigger change event on captured listener)
       - Test: cleans up listener on unmount (renderHook result.unmount, verify removeEventListener called)
  </action>
  <verify>
    <automated>npx vitest run components/data/__tests__/chips.test.ts components/hooks/useReducedMotion.test.ts --reporter=verbose</automated>
  </verify>
  <done>M4 chips have tops: 38, non-M4 chips have undefined tops, useReducedMotion hook detects and reactively tracks prefers-reduced-motion, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useInView + useCountUp hooks</name>
  <files>components/hooks/useInView.ts, components/hooks/useInView.test.ts, components/hooks/useCountUp.ts, components/hooks/useCountUp.test.ts</files>
  <behavior>
    - useInView: returns false before element enters viewport
    - useInView: returns true after IntersectionObserver fires with isIntersecting: true
    - useInView: disconnects observer after first intersection (one-shot)
    - useInView: disconnects observer on unmount cleanup
    - useCountUp: returns 0 when trigger is false
    - useCountUp: animates from 0 toward target when trigger is true
    - useCountUp: reaches exact target value when animation completes
    - useCountUp: cancels animation frame on unmount
  </behavior>
  <action>
    **useInView hook (A11Y-04):**
    1. Create `components/hooks/useInView.ts` following 03-RESEARCH.md Pattern 2:
       - Accept `ref: RefObject<Element | null>` and `threshold: number` (default 0.3) as primitive params (not options object -- avoids infinite re-render pitfall)
       - Create IntersectionObserver in useEffect, observe ref.current
       - On intersection (isIntersecting === true): set state to true, call observer.disconnect() (one-shot pattern)
       - Cleanup: disconnect observer on unmount
       - Return boolean
    2. Create `components/hooks/useInView.test.ts`:
       - Mock IntersectionObserver globally in beforeEach (store callback, return mock with observe/disconnect/unobserve as vi.fn())
       - Use `vi.stubGlobal('IntersectionObserver', mockConstructor)` pattern
       - Create a mock ref using `{ current: document.createElement('div') }`
       - Test: returns false initially
       - Test: returns true after simulating intersection (call stored callback with `[{ isIntersecting: true }]`)
       - Test: disconnect called after intersection (one-shot)
       - Test: disconnect called on unmount

    **useCountUp hook (prep for CHIP-03 in Phase 4):**
    3. Create `components/hooks/useCountUp.ts` following 03-RESEARCH.md Pattern 1:
       - Signature: `useCountUp(target: number, duration = 1200, trigger = true): number`
       - When trigger is false: return 0, do not start animation
       - When trigger is true: start rAF loop with ease-out cubic: `1 - Math.pow(1 - progress, 3)`
       - Store rAF ID in useRef, cancel in useEffect cleanup (prevents unmount leak)
       - Return Math.round(target * eased) during animation, exact target at completion
    4. Create `components/hooks/useCountUp.test.ts`:
       - Mock requestAnimationFrame and cancelAnimationFrame using vi.spyOn or vi.stubGlobal
       - Use a manual rAF mock that captures callbacks and allows stepping: `vi.spyOn(window, 'requestAnimationFrame').mockImplementation(cb => { storedCallback = cb; return id++; })`
       - Test: returns 0 when trigger is false
       - Test: calls requestAnimationFrame when trigger is true
       - Test: reaches target value when progress = 1 (simulate timestamp where (ts - start) >= duration)
       - Test: calls cancelAnimationFrame on unmount (result.unmount, verify cancel called)
  </action>
  <verify>
    <automated>npx vitest run components/hooks/useInView.test.ts components/hooks/useCountUp.test.ts --reporter=verbose</automated>
  </verify>
  <done>useInView detects viewport entry and disconnects (one-shot), useCountUp animates 0-to-target with easing and cleanup, all tests green</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
npx vitest run --reporter=verbose
```
All tests green including existing chip data tests, new TOPS assertions, and all three hook test files.
</verification>

<success_criteria>
- Chip interface has optional tops field, M4 entries have tops: 38
- useReducedMotion reactively detects prefers-reduced-motion
- useInView fires once on viewport entry then disconnects
- useCountUp animates from 0 to target with ease-out cubic
- All hooks clean up resources on unmount
- Full test suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-animation-infrastructure/03-01-SUMMARY.md`
</output>

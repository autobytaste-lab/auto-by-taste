---
phase: 02-mac-hardware-data
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - components/ChipComparison.tsx
  - components/__tests__/ChipComparison.test.tsx
autonomous: true
requirements:
  - R4

must_haves:
  truths:
    - "User can see visual comparison of M1 vs M2 vs M3 vs M4 generations"
    - "Key metrics displayed: CPU cores, GPU cores, memory bandwidth, max RAM"
    - "Memory bandwidth comparison clearly shows progression"
    - "Chart is responsive and readable on all screens"
    - "Vietnamese labels used throughout"
  artifacts:
    - path: "components/ChipComparison.tsx"
      provides: "M-series chip comparison component with Recharts"
      exports: ["ChipComparison"]
      min_lines: 60
    - path: "components/__tests__/ChipComparison.test.tsx"
      provides: "Component render tests"
      contains: "ChipComparison"
  key_links:
    - from: "components/ChipComparison.tsx"
      to: "components/data/chips.ts"
      via: "import chips data"
      pattern: "import.*chips.*from.*data/chips"
    - from: "components/ChipComparison.tsx"
      to: "recharts"
      via: "chart components"
      pattern: "import.*BarChart.*ResponsiveContainer.*from.*recharts"
---

<objective>
Build M-series chip comparison component with visual charts.

Purpose: Help users understand the progression and differences between M1, M2, M3, and M4 chip generations. This addresses R4 (M-Series Chip Comparison) requirement.

Output: ChipComparison React component using Recharts, following existing ModelHardwareGraph.tsx patterns, with Vietnamese labels and glassmorphism styling.
</objective>

<execution_context>
@/home/anonymouse/.claude/get-shit-done/workflows/execute-plan.md
@/home/anonymouse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mac-hardware-data/02-RESEARCH.md
@.planning/phases/02-mac-hardware-data/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From components/data/types.ts (created in Plan 01):
```typescript
export type ChipGeneration = 'M1' | 'M2' | 'M3' | 'M4';
export type ChipVariant = 'base' | 'Pro' | 'Max' | 'Ultra';

export interface Chip {
  readonly id: string;
  readonly name: string;
  readonly generation: ChipGeneration;
  readonly variant: ChipVariant;
  readonly cpuCores: {
    performance: number;
    efficiency: number;
    total: number;
  };
  readonly gpuCores: number;
  readonly neuralEngineCores: number;
  readonly memoryBandwidth: number; // GB/s
  readonly maxMemory: number; // GB
  readonly processNode: string;
}
```

From components/data/chips.ts (created in Plan 01):
```typescript
export const chips: readonly Chip[] = [...]; // 15 chips
export const getChipById = (id: string): Chip | undefined => {...};
```

<!-- Existing Recharts patterns -->

From components/ModelHardwareGraph.tsx:
```typescript
import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Cell } from 'recharts';

// Chart styling pattern
<ResponsiveContainer width="100%" height="100%">
  <BarChart data={data}>
    <CartesianGrid strokeDasharray="3 3" stroke="#334155" vertical={false} />
    <XAxis dataKey="name" stroke="#94a3b8" fontSize={12} />
    <YAxis stroke="#94a3b8" fontSize={12} />
    <Tooltip
      contentStyle={{ backgroundColor: '#0f172a', border: '1px solid #1e293b' }}
    />
    <Bar dataKey="value" radius={[8, 8, 0, 0]} />
  </BarChart>
</ResponsiveContainer>

// Glass card styling
<div className="glass-card p-6 lg:p-10 rounded-[2.5rem] border border-white/5">
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ChipComparison component with tests</name>
  <files>
    components/ChipComparison.tsx
    components/__tests__/ChipComparison.test.tsx
  </files>
  <behavior>
    - Test: Component renders without crashing
    - Test: Component displays chart title in Vietnamese
    - Test: Chart contains bars for CPU cores, GPU cores, memory bandwidth, max RAM
    - Test: All chip generations (M1, M2, M3, M4) represented in chart data
    - Test: Component applies glass-card styling
    - Test: ResponsiveContainer used for responsive chart sizing
  </behavior>
  <action>
    1. Create components/__tests__/ directory if not exists
    2. Write ChipComparison.test.tsx FIRST with render tests:
       - Import from @testing-library/react
       - Test component renders
       - Test Vietnamese title present
       - Test chart elements present
    3. Create ChipComparison.tsx:
       - Import chips from './data/chips'
       - Transform chip data for Recharts (group by generation for comparison)
       - Use existing ModelHardwareGraph.tsx patterns for styling
       - Add Vietnamese title "So sanh chip M-series" (with accent: "So sánh chip M-series")
       - Apply glass-card styling with rounded-[2.5rem]
       - Use brand colors: blue (#60A5FA, #3B82F6, #2563EB), purple (#8B5CF6)
       - Include legend for metric types
    4. Run tests to verify component renders correctly

    Chart data transformation:
    - Filter to base/Pro/Max variants per generation (exclude Ultra for cleaner comparison)
    - Or show one representative chip per generation
    - Scale memory bandwidth for chart visibility (divide by 10 or show in hundreds)

    Vietnamese labels:
    - "CPU Cores" -> "Nhân CPU"
    - "GPU Cores" -> "Nhân GPU"
    - "Memory Bandwidth" -> "Băng thông bộ nhớ (GB/s)"
    - "Max RAM" -> "RAM tối đa (GB)"
  </action>
  <verify>
    <automated>npm test -- ChipComparison.test.tsx -x</automated>
  </verify>
  <done>
    - ChipComparison.tsx renders Recharts bar chart
    - Chart shows CPU cores, GPU cores, memory bandwidth, max RAM
    - All M-series generations represented
    - Vietnamese labels used
    - Glass-card styling applied
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add chip selection and details panel</name>
  <files>
    components/ChipComparison.tsx
  </files>
  <action>
    1. Add useState for selected chip (activeIndex pattern from ModelHardwareGraph.tsx)
    2. Add chip selector grid below chart (similar to ModelHardwareGraph selector)
    3. Add details panel showing selected chip specifications:
       - Chip name with generation badge
       - CPU cores breakdown (performance + efficiency)
       - GPU cores count
       - Memory bandwidth with visual indicator
       - Max RAM capacity
       - Process node (5nm, 3nm, etc.)
    4. Use onClick handler on chart bars to select chip
    5. Apply existing gradient card styling from ModelHardwareGraph.tsx:
       - bg-gradient-to-br from-blue-600/10 to-purple-600/10
       - border border-blue-500/20
       - shadow-2xl shadow-blue-500/5
    6. Ensure Vietnamese text throughout

    Key interactions:
    - Click on bar selects that chip
    - Click on selector button selects chip
    - Details panel updates immediately on selection
  </action>
  <verify>
    <automated>npm test -- ChipComparison.test.tsx -x</automated>
  </verify>
  <done>
    - Chip selection works via chart click and selector buttons
    - Details panel shows all chip specifications
    - Styling matches existing glassmorphism theme
    - Interactive elements are responsive
  </done>
</task>

<task type="auto">
  <name>Task 3: Add generation comparison view</name>
  <files>
    components/ChipComparison.tsx
  </files>
  <action>
    1. Add toggle/tab to switch between:
       - "All Chips" - full comparison view (current)
       - "By Generation" - grouped comparison (M1 vs M2 vs M3 vs M4)
    2. For "By Generation" view:
       - Show one representative chip per generation (base or most popular variant)
       - Add performance uplift percentages between generations
       - Show memory bandwidth progression clearly
    3. Add comparison highlights:
       - M3 Pro bandwidth regression note (150GB/s vs M2 Pro 200GB/s)
       - M4 Max as "fastest for AI" indicator
    4. Use simple tab styling:
       - Active: bg-blue-600 text-white
       - Inactive: glass-card text-slate-400
    5. Verify chart updates correctly when switching views

    Performance uplift examples (can be approximate):
    - M2 vs M1: ~15-20% faster
    - M3 vs M2: ~25-30% faster
    - M4 vs M3: ~20-30% faster
  </action>
  <verify>
    <automated>npm test -- ChipComparison.test.tsx -x</automated>
  </verify>
  <done>
    - View toggle switches between "All Chips" and "By Generation"
    - Performance uplift indicators shown in generation view
    - M3 Pro bandwidth regression noted
    - M4 Max highlighted for AI workloads
    - All tests pass
  </done>
</task>

</tasks>

<verification>
Run full test suite:
```bash
npm test -- --run
```

Visual verification (requires browser):
1. Open dev server: `npm run dev`
2. Navigate to page with ChipComparison component
3. Verify chart renders with correct data
4. Test chip selection interactions
5. Test view toggle
6. Test on mobile viewport (responsive sizing)
</verification>

<success_criteria>
- [ ] ChipComparison component renders correctly
- [ ] Chart displays CPU cores, GPU cores, memory bandwidth, max RAM
- [ ] All M-series generations (M1-M4) represented
- [ ] Chip selection updates details panel
- [ ] View toggle switches between all chips and by-generation views
- [ ] Vietnamese labels throughout
- [ ] Glass-card styling matches existing components
- [ ] Responsive on mobile viewports
- [ ] All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-mac-hardware-data/02-02-SUMMARY.md`
</output>

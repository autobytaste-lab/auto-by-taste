---
phase: 02-mac-hardware-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/data/types.ts
  - components/data/chips.ts
  - components/data/macModels.ts
  - components/data/aiModels.ts
  - components/data/compatibility.ts
  - components/data/__tests__/chips.test.ts
  - components/data/__tests__/macModels.test.ts
  - components/data/__tests__/aiModels.test.ts
  - components/data/__tests__/compatibility.test.ts
autonomous: true
requirements:
  - R3

must_haves:
  truths:
    - "All M-series chips from M1 through M4 Max have accurate specifications"
    - "All current Mac models (2020+) are represented with correct RAM options"
    - "AI models have correct RAM requirements for Q4_K_M quantization"
    - "Compatibility function correctly filters AI models by Mac RAM capacity"
  artifacts:
    - path: "components/data/types.ts"
      provides: "TypeScript interfaces for Chip, MacModel, AIModel"
      exports: ["Chip", "MacModel", "AIModel", "ChipGeneration", "ChipVariant", "MacCategory", "AIModelFamily"]
    - path: "components/data/chips.ts"
      provides: "M-series chip specifications array"
      exports: ["chips", "getChipById"]
    - path: "components/data/macModels.ts"
      provides: "Mac model data array"
      exports: ["macModels", "getMacById", "getMacsByCategory"]
    - path: "components/data/aiModels.ts"
      provides: "AI model RAM requirements array"
      exports: ["aiModels", "getAIModelById"]
    - path: "components/data/compatibility.ts"
      provides: "Compatibility matching function"
      exports: ["getCompatibleModels", "CompatibilityResult"]
  key_links:
    - from: "components/data/macModels.ts"
      to: "components/data/chips.ts"
      via: "chipId reference"
      pattern: "chipId.*:.*string"
    - from: "components/data/compatibility.ts"
      to: "components/data/chips.ts"
      via: "chip lookup"
      pattern: "chips\\.find"
    - from: "components/data/compatibility.ts"
      to: "components/data/aiModels.ts"
      via: "RAM filtering"
      pattern: "aiModels\\.filter"
---

<objective>
Create structured TypeScript data for Mac models, M-series chips, and AI model compatibility.

Purpose: Establish the data layer foundation for the Mac Model Selector (Phase 3) and Chip Comparison component (Plan 02). This data will power the user-facing interactive features.

Output: Type-safe data arrays for all M-series chips (M1-M4), current Mac models (2020+), and AI models with RAM requirements, plus a compatibility function to filter AI models by Mac RAM capacity.
</objective>

<execution_context>
@/home/anonymouse/.claude/get-shit-done/workflows/execute-plan.md
@/home/anonymouse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-mac-hardware-data/02-RESEARCH.md

<interfaces>
<!-- Existing patterns from the codebase -->

From components/ModelHardwareGraph.tsx:
```typescript
// Existing interface pattern for AI model data
interface ModelData {
  name: string;
  ram: number;
  hardware: string;
  description: string;
  color: string;
}
```

From components/particles/particleConfig.test.ts:
```typescript
// Testing pattern using Vitest
import { describe, it, expect, vi, beforeEach } from 'vitest';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TypeScript interfaces and chip data with tests</name>
  <files>
    components/data/types.ts
    components/data/chips.ts
    components/data/__tests__/chips.test.ts
  </files>
  <behavior>
    - Test: chips array contains all M1 variants (base, Pro, Max, Ultra)
    - Test: chips array contains all M2 variants (base, Pro, Max, Ultra)
    - Test: chips array contains all M3 variants (base, Pro, Max)
    - Test: chips array contains all M4 variants (base, Pro, Max)
    - Test: M3 Pro has LOWER bandwidth than M2 Pro (150 vs 200 GB/s - known regression)
    - Test: M1 Ultra has double the bandwidth of M1 Max (800 vs 400 GB/s)
    - Test: getChipById returns correct chip or undefined
    - Test: All chips have required properties (id, name, generation, variant, cpuCores, gpuCores, etc.)
  </behavior>
  <action>
    1. Create components/data/ directory
    2. Create types.ts with interfaces:
       - ChipGeneration = 'M1' | 'M2' | 'M3' | 'M4'
       - ChipVariant = 'base' | 'Pro' | 'Max' | 'Ultra'
       - Chip interface with readonly properties: id, name, generation, variant, cpuCores (performance, efficiency, total), gpuCores, neuralEngineCores, memoryBandwidth (GB/s), maxMemory (GB), processNode
    3. Create chips.ts with complete M-series chip data from RESEARCH.md (15 chips total)
    4. Add getChipById helper function
    5. Write tests FIRST following existing particleConfig.test.ts patterns
    6. Run tests to verify chip data integrity

    Use exact specifications from 02-RESEARCH.md "Example 1: Complete Chip Data Structure"
  </action>
  <verify>
    <automated>npm test -- chips.test.ts -x</automated>
  </verify>
  <done>
    - types.ts exports Chip interface with all properties
    - chips.ts exports 15 M-series chips with accurate specs
    - All tests pass validating chip data integrity
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Mac models and AI models data with tests</name>
  <files>
    components/data/macModels.ts
    components/data/aiModels.ts
    components/data/__tests__/macModels.test.ts
    components/data/__tests__/aiModels.test.ts
  </files>
  <behavior>
    - Test: macModels array covers MacBook Air, MacBook Pro, Mac Mini, Mac Studio, Mac Pro categories
    - Test: All Mac models have valid chipId references (exist in chips array)
    - Test: Mac Mini M4 starts at 16GB RAM (not 8GB - Oct 2024 update)
    - Test: MacBook Air M3 starts at 16GB RAM (not 8GB)
    - Test: getMacById returns correct Mac or undefined
    - Test: getMacsByCategory returns array of Macs for given category
    - Test: aiModels covers small (8GB), mid (16GB), large (24GB+), and extreme (64GB+) tiers
    - Test: All aiModels have description and useCase in Vietnamese
    - Test: Q4_K_M quantization specified for all models
    - Test: getAIModelById returns correct model or undefined
  </behavior>
  <action>
    1. Add MacCategory and MacModel interface to types.ts:
       - MacCategory = 'MacBook Air' | 'MacBook Pro' | 'Mac Mini' | 'Mac Studio' | 'Mac Pro'
       - MacModel interface: id, name, category, chipId, releaseYear, ramOptions (readonly number[])
    2. Add AIModelFamily and AIModel interface to types.ts:
       - AIModelFamily = 'Llama' | 'Mistral' | 'Phi' | 'Qwen' | 'Gemma' | 'DeepSeek'
       - AIModel interface: id, name, family, parameters, minRamGB, quantization, description, useCase
    3. Create macModels.ts with 18+ Mac models from RESEARCH.md "Example 2"
    4. Add getMacById and getMacsByCategory helpers
    5. Create aiModels.ts with 12+ AI models from RESEARCH.md "Example 3"
    6. Add getAIModelById helper
    7. Write tests FIRST, then implement

    IMPORTANT: Verify RAM options match current Apple specs:
    - Mac Mini M4 2024: 16/24/32 GB (NOT 8GB)
    - MacBook Air M3: 16/24 GB (NOT 8GB)
  </action>
  <verify>
    <automated>npm test -- macModels.test.ts aiModels.test.ts -x</automated>
  </verify>
  <done>
    - macModels.ts exports 18+ Mac models with correct RAM configurations
    - aiModels.ts exports 12+ AI models with Vietnamese descriptions
    - All chipId references validated against chips array
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create compatibility function with tests</name>
  <files>
    components/data/compatibility.ts
    components/data/__tests__/compatibility.test.ts
  </files>
  <behavior>
    - Test: getCompatibleModels returns compatible AI models for Mac Mini M4 with 16GB
    - Test: getCompatibleModels separates compatible from requiresUpgrade models
    - Test: compatible models all have minRamGB <= selected RAM
    - Test: requiresUpgrade models have minRamGB > selected RAM but <= chip.maxMemory
    - Test: Returns correct chip information in result
    - Test: Throws error for invalid Mac with nonexistent chipId
    - Test: Mac Studio M2 Ultra with 128GB can run 70B models
    - Test: MacBook Air M1 with 8GB only gets small models (Phi-3, Gemma 2B)
  </behavior>
  <action>
    1. Add CompatibilityResult interface to types.ts:
       - compatible: AIModel[]
       - requiresUpgrade: AIModel[]
       - chip: Chip
       - maxRam: number
    2. Create compatibility.ts with getCompatibleModels function:
       - Input: mac (MacModel), selectedRam (number)
       - Look up chip from chips array by mac.chipId
       - Filter aiModels by minRamGB <= selectedRam for compatible
       - Filter aiModels where minRamGB > selectedRam AND minRamGB <= chip.maxMemory for requiresUpgrade
       - Return CompatibilityResult with chip info and maxRam
       - Throw error if chip not found
    3. Write tests FIRST following pattern from 02-RESEARCH.md "Example 4"

    Reference implementation from RESEARCH.md Pattern 3.
  </action>
  <verify>
    <automated>npm test -- compatibility.test.ts -x</automated>
  </verify>
  <done>
    - compatibility.ts exports getCompatibleModels function
    - Function correctly filters AI models by RAM capacity
    - Function separates compatible vs requiresUpgrade
    - All edge cases tested (invalid chip, boundary RAM values)
    - All tests pass
  </done>
</task>

</tasks>

<verification>
Run full test suite for data layer:
```bash
npm test -- components/data -x
```

Expected: All tests pass, no TypeScript errors.
</verification>

<success_criteria>
- [ ] TypeScript interfaces defined for Chip, MacModel, AIModel, CompatibilityResult
- [ ] 15 M-series chips with accurate specifications (verified against Apple sources)
- [ ] 18+ Mac models with current RAM configurations (16GB base for M4 Macs)
- [ ] 12+ AI models with Q4_K_M quantization RAM requirements
- [ ] getCompatibleModels function filters correctly by RAM
- [ ] All tests pass (chips, macModels, aiModels, compatibility)
- [ ] No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-mac-hardware-data/02-01-SUMMARY.md`
</output>

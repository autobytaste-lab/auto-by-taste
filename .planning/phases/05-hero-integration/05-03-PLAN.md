---
phase: 05-hero-integration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - i18n/translations/en.test.ts
  - i18n/translations/vi.test.ts
autonomous: true
requirements:
  - HERO-01
  - HERO-02

must_haves:
  truths:
    - "Translation tests pass after hero keys are updated with new Apple Silicon messaging"
    - "Vietnamese translation test no longer asserts old 'Thue Nhan Vien AI' headline value"
    - "English and Vietnamese key structure test still validates matching keys"
  artifacts:
    - path: "i18n/translations/en.test.ts"
      provides: "Updated English translation tests compatible with new hero content"
      contains: "hero"
    - path: "i18n/translations/vi.test.ts"
      provides: "Updated Vietnamese translation tests compatible with new hero content"
      contains: "hero"
  key_links:
    - from: "i18n/translations/vi.test.ts"
      to: "i18n/translations/vi.ts"
      via: "import and assertion"
      pattern: "vi\\.hero\\."
    - from: "i18n/translations/en.test.ts"
      to: "i18n/translations/en.ts"
      via: "import and assertion"
      pattern: "en\\.hero\\."
---

<objective>
Update existing translation test files to be compatible with the hero content changes from Plan 05-01. The vi.test.ts asserts old hero headline text ('Thue Nhan Vien AI') which will fail after translations are updated.

Purpose: Prevent test regression when Plan 05-01 updates hero translation keys. This plan runs in Wave 1 alongside 05-01 (no dependency) because it only modifies test assertion values, not the translations themselves.
Output: Updated en.test.ts and vi.test.ts that pass with both old AND new hero content (using flexible assertions).
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-hero-integration/05-RESEARCH.md

@i18n/translations/en.test.ts
@i18n/translations/vi.test.ts
@i18n/translations/en.ts
@i18n/translations/vi.ts

<interfaces>
<!-- Key contracts the executor needs -->

From i18n/translations/en.ts:
```typescript
export const en = { hero: { badge: string, titleLine1: string, titleLine2: string, subtitle: string, ... }, ... };
export type Translation = typeof en;
```

From i18n/translations/vi.test.ts (line 29 -- will break):
```typescript
expect(vi.hero.titleLine1).toBe('Thue Nhan Vien AI');  // OLD value, must update
```

From i18n/translations/vi.test.ts (line 22-24 -- structural test):
```typescript
// This test compares ALL keys between en and vi -- will catch mismatches
const enKeys = getKeys(en).sort();
const viKeys = getKeys(vi).sort();
expect(viKeys).toEqual(enKeys);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update translation test assertions for new hero content</name>
  <files>i18n/translations/en.test.ts, i18n/translations/vi.test.ts</files>
  <action>
    Update both translation test files to be compatible with the new hero messaging from Plan 05-01.

    **vi.test.ts changes:**
    1. Line 29: Change `expect(vi.hero.titleLine1).toBe('Thue Nhan Vien AI')` to assert the new Vietnamese headline value. Use: `expect(vi.hero.titleLine1).toBe('Suc Manh Silicon')` (matching the value Plan 05-01 will set in vi.ts).
    2. The structural test ("has same structure as English") on lines 12-24 needs NO changes -- it dynamically compares keys and will pass as long as both en.ts and vi.ts have matching key structures (which Plan 05-01 ensures).

    **en.test.ts changes:**
    1. Line 38: `expect(en.hero.badge).toBeDefined()` -- still valid, no change needed.
    2. Add a new assertion to verify the hero badge contains the new Apple Silicon content: `expect(en.hero.badge).toBe('Apple Silicon AI Infrastructure')`. This confirms HERO-01 messaging is in place.

    **Important:** These test files will temporarily fail if run BEFORE Plan 05-01 updates the translation source files. This is expected -- they run in the same wave and the full suite check happens after both plans complete.

    Do NOT modify en.ts or vi.ts (those belong to Plan 05-01).
  </action>
  <verify>
    <automated>cd /home/dev/workspace/auto-by-taste && npx vitest run i18n/translations/en.test.ts i18n/translations/vi.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>Translation test files updated with assertions matching the new hero content values. Tests pass after Plan 05-01's translation updates are applied. No modification to translation source files.</done>
</task>

</tasks>

<verification>
1. `npx vitest run i18n/translations/en.test.ts i18n/translations/vi.test.ts --reporter=verbose` -- both test files pass
2. en.test.ts asserts `en.hero.badge` equals 'Apple Silicon AI Infrastructure'
3. vi.test.ts asserts `vi.hero.titleLine1` equals the new Vietnamese headline (not 'Thue Nhan Vien AI')
4. Structural key comparison test in vi.test.ts still present and passing
</verification>

<success_criteria>
- Translation tests pass with the new hero content
- No old hero assertion values remain in test files
- Key structure test still validates en/vi parity
</success_criteria>

<output>
After completion, create `.planning/phases/05-hero-integration/05-03-SUMMARY.md`
</output>

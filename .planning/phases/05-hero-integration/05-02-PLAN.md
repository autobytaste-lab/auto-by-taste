---
phase: 05-hero-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified: []
autonomous: false
requirements:
  - HERO-04
  - HERO-05

must_haves:
  truths:
    - "Headline is the most visually prominent element when page loads (larger than diagram, badges, CTAs)"
    - "ChipDiagram is visible and functional (tabs work, blocks visible) within the Hero section"
    - "Spec callout badges are readable and scannable at a glance"
    - "Layout works on mobile (320px) without horizontal scrolling or overlapping"
    - "No visual regressions in sections below Hero"
---

<objective>
Visually verify the redesigned Hero section renders correctly across viewports -- headline hierarchy, badge readability, ChipDiagram placement, and mobile responsiveness.

Purpose: Confirm the layout composition achieves proper visual hierarchy and the chip diagram supports (not overwhelms) the headline messaging.
Output: Human-verified visual approval or issue list for fixes.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run full test suite</name>
  <files></files>
  <action>
    1. Run `npx vitest run` to confirm all tests pass (full suite, not just Hero)
    2. Start the Vite dev server: `npx vite --host 0.0.0.0 --port 5173`
    3. Report the dev server URL for visual inspection
  </action>
  <verify>
    <automated>cd /home/dev/workspace/auto-by-taste && npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>All tests pass. Dev server running and accessible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Hero redesign</name>
  <files></files>
  <action>
    Present the dev server URL to the user for visual inspection of the redesigned Hero section. User verifies:
    1. Desktop: headline prominence, CTA visibility, spec badge readability, ChipDiagram placement and functionality
    2. Mobile (320px): no overflow, proper wrapping, readable diagram
    3. Page scroll: no regressions in sections below Hero
  </action>
  <verify>User confirms visual approval</verify>
  <done>User types "approved" confirming Hero redesign meets visual hierarchy and responsiveness requirements</done>
</task>

</tasks>

<verification>
Human visual approval of Hero section on desktop and mobile viewports.
</verification>

<success_criteria>
- Visual hierarchy confirmed: headline > CTAs > badges > diagram
- ChipDiagram functional within Hero (tabs switch variants)
- Mobile layout works at 320px without overflow
- No regressions in page sections below Hero
</success_criteria>

<output>
After completion, create `.planning/phases/05-hero-integration/05-02-SUMMARY.md`
</output>

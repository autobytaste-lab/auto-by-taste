---
phase: 01-i18n-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - test/setup.ts
  - i18n/translations/en.ts
  - i18n/translations/vi.ts
  - i18n/translations/en.test.ts
  - i18n/translations/vi.test.ts
autonomous: true
requirements: [I18N-01, I18N-02, I18N-03, CONT-01, CONT-03]

must_haves:
  truths:
    - "English translation file exists with all content from 9 components"
    - "Vietnamese translation file exists with all existing content preserved"
    - "Translation files use type-safe TypeScript structure"
    - "All translatable strings extracted (text, aria-labels, placeholders, alt text)"
    - "Test infrastructure runs successfully"
  artifacts:
    - path: "i18n/translations/en.ts"
      provides: "English translations for all 9 components"
      exports: ["en", "Translation"]
      min_lines: 150
    - path: "i18n/translations/vi.ts"
      provides: "Vietnamese translations matching EN structure"
      exports: ["vi"]
      min_lines: 150
    - path: "vitest.config.ts"
      provides: "Test framework configuration"
      contains: "jsdom"
    - path: "test/setup.ts"
      provides: "Test setup with jest-dom matchers"
      contains: "@testing-library/jest-dom"
  key_links:
    - from: "i18n/translations/vi.ts"
      to: "i18n/translations/en.ts"
      via: "imports Translation type"
      pattern: "import.*Translation.*from.*en"
    - from: "package.json"
      to: "vitest.config.ts"
      via: "test script"
      pattern: "vitest"
---

<objective>
Create translation infrastructure: English and Vietnamese translation files with all content extracted from 9 components, plus test infrastructure.

Purpose: Establishes the foundation for i18n by centralizing all translatable strings in type-safe TypeScript files. Wave 0 test setup enables verification throughout Phase 1.

Output: Translation files (en.ts, vi.ts) with nested key structure, TypeScript type enforcement, and working Vitest test infrastructure.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-i18n-foundation/01-RESEARCH.md
@.planning/phases/01-i18n-foundation/01-VALIDATION.md

<interfaces>
<!-- Existing component strings to extract (all hardcoded Vietnamese) -->

From components/Navbar.tsx:
- "AI-Local Hub" (title)
- "Tong quan", "Giai phap", "Goi san pham", "Mo hinh", "Goi von" (nav links)
- "Zalo Founder", "Dau tu ngay" (buttons)

From components/Hero.tsx:
- "Apple Silicon AI Agent Platform" (badge)
- "Ky Nguyen AI Local Agent." (title)
- "Bien Mac Mini & Mac Studio..." (subtitle)
- "Tham gia dau tu", "Trai nghiem AI Agent" (CTAs)
- "AI Local Hub Agent on Macmini M4" (alt text)

From components/ProductTiers.tsx:
- "Cac Goi Giai Phap" (heading)
- "Thiet ke linh hoat theo nhu cau..." (description)
- "Pho bien nhat" (badge)
- "Phan cung", "Doi tuong", "Kha nang xu ly" (labels)
- "Lien he Tu van" (button)
- Tier data: Basic/Pro/Enterprise names, hardware specs, targets, capabilities

From components/ProblemSolution.tsx:
- "Tai sao la bay gio?" (heading)
- Problem cards: "Nguy co ro ri du lieu", "Chi phi van hanh khong lo"
- Solution section: "Giai phap: Local AI tren Apple Silicon"
- Checkmarks: "Bao mat 100%...", "Phan hoi cuc nhanh...", "Chi phi dau tu mot lan..."
- "Cam ket vang", "Du lieu cua ban o lai voi ban..."

From components/TargetSegments.tsx:
- "Phan Khuc Muc Tieu" (heading)
- "Chung toi tap trung vao nhung linh vuc..." (description)
- "Nhu cau trong tam" (label)
- "Tai sao chon Apple Silicon?" (label)
- 3 segment cards with titles, descriptions, needs arrays, whyMac text

From components/BusinessModel.tsx:
- "Mo Hinh Kinh Doanh" (heading)
- 4 business model cards with titles, descriptions, highlights
- "So huu tri tue nhan tao rieng" (banner title)
- Stats: "#1 Bao mat", "10x Hieu suat", "0$ Cloud Fee"

From components/StrategySection.tsx:
- "Chien Luoc Chao Ban" (heading)
- Strategy 1: "Dung thu voi du lieu that"
- Strategy 2: "Bai toan chi phi (ROI)"
- "Truyen thong" vs "AI-Local Hub" labels
- "Thong diep chu chot: So huu tri tue nhan tao rieng..."

From components/FundingSection.tsx:
- "Ke Hoach Su Dung Von" (heading)
- Pie chart labels: "R&D...", "Thiet bi & Kho hang", "Marketing & Sales", "Van hanh & Phap ly"
- "Tai Ban Ke Hoach Chi Tiet (PDF)" (button)

From components/Footer.tsx:
- "AI-Local Hub" (brand)
- Company description paragraph
- "Zalo Founder", "WhatsApp" (links)
- "Lien ket" section: "Ve chung toi", "Goi giai phap", "Quan he nha dau tu", "Tuyen dung"
- "Lien he truc tiep" section: address, email, phone
- Copyright, "Dieu khoan dich vu", "Chinh sach bao mat"

From App.tsx:
- "Chat Zalo Founder" (floating button)
- "Zalo" (alt text)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vitest and create test infrastructure</name>
  <files>package.json, vitest.config.ts, test/setup.ts</files>
  <action>
Install test dependencies:
```bash
npm install -D vitest @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom @types/react @types/react-dom
```

Create vitest.config.ts at project root:
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./test/setup.ts'],
    include: ['**/*.test.{ts,tsx}'],
  },
});
```

Create test/setup.ts:
```typescript
import '@testing-library/jest-dom';
```

Add test script to package.json scripts:
```json
"test": "vitest"
```

Verify installation works by running `npm test -- --run` (should exit cleanly with no tests found).
  </action>
  <verify>
    <automated>npm test -- --run 2>&1 | grep -E "(No test files found|0 passed)" && echo "PASS: Vitest installed"</automated>
  </verify>
  <done>Vitest installed, configured with jsdom, test script in package.json, `npm test -- --run` exits without error</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create English translation file with all component strings</name>
  <files>i18n/translations/en.ts, i18n/translations/en.test.ts</files>
  <behavior>
    - Test: en object exports all required top-level keys (navbar, hero, problemSolution, targetSegments, productTiers, businessModel, strategy, funding, footer, floatingButton)
    - Test: All leaf values are non-empty strings (no empty strings allowed)
    - Test: Translation type is exported
    - Test: Nested keys accessible (e.g., en.navbar.title exists)
  </behavior>
  <action>
Create i18n/translations/en.test.ts first (RED):
```typescript
import { describe, it, expect } from 'vitest';
import { en, Translation } from './en';

describe('English translations', () => {
  it('exports all required top-level keys', () => {
    const requiredKeys = [
      'navbar', 'hero', 'problemSolution', 'targetSegments',
      'productTiers', 'businessModel', 'strategy', 'funding',
      'footer', 'floatingButton'
    ];
    requiredKeys.forEach(key => {
      expect(en).toHaveProperty(key);
    });
  });

  it('has no empty string values', () => {
    const checkNoEmptyStrings = (obj: Record<string, any>, path = ''): void => {
      Object.entries(obj).forEach(([key, value]) => {
        const currentPath = path ? `${path}.${key}` : key;
        if (typeof value === 'string') {
          expect(value.trim(), `Empty string at ${currentPath}`).not.toBe('');
        } else if (typeof value === 'object' && value !== null) {
          checkNoEmptyStrings(value, currentPath);
        }
      });
    };
    checkNoEmptyStrings(en);
  });

  it('exports Translation type', () => {
    const typed: Translation = en;
    expect(typed).toBeDefined();
  });

  it('has nested keys accessible', () => {
    expect(en.navbar.title).toBe('AI-Local Hub');
    expect(en.hero.badge).toBeDefined();
    expect(en.productTiers.heading).toBeDefined();
  });
});
```

Then create i18n/translations/en.ts (GREEN):

Structure the file with nested objects organized by component. Use 2-3 level nesting max.
Key naming: camelCase, component name as top level, logical groupings within.

Include ALL strings from all 9 components plus App.tsx floating button:
- navbar: title, overview, solution, products, business, funding, zaloLink, investButton
- hero: badge, titleLine1, titleLine2, subtitle, ctaPrimary, ctaSecondary, imageAlt
- problemSolution: heading, description, problem1Title, problem1Description, problem2Title, problem2Description, solutionTitle, solutionDescription, check1, check2, check3, commitmentTitle, commitmentQuote, solutionImageAlt
- targetSegments: heading, description, needsLabel, whyAppleLabel, segment1 (title, description, need1-3, whyMac), segment2 (...), segment3 (...)
- productTiers: heading, description, popularBadge, hardwareLabel, targetLabel, capabilityLabel, ctaButton, basic (name, hardware, target, capability), pro (...), enterprise (...)
- businessModel: heading, description, model1-4 (title, description, highlight), bannerTitle, bannerQuote, stat1Label, stat2Label, stat3Label
- strategy: heading, description, strategy1Title, strategy1Desc, strategy1Quote, strategy2Title, strategy2Desc, traditionalLabel, aiLocalLabel, traditionalValue, aiLocalValue, keyMessage
- funding: heading, description, categories (rd, equipment, marketing, operations with name and value), downloadButton
- footer: brand, brandDescription, zaloLink, whatsappLink, linksTitle, aboutUs, solutions, investors, careers, contactTitle, address, email, founderLabel, phone, copyright, terms, privacy
- floatingButton: label, zaloAlt

Use English translations for all text. For Vietnamese-specific content (company name, locations), keep as-is or use standard English equivalents.

Export: `export const en = { ... } as const;` and `export type Translation = typeof en;`
  </action>
  <verify>
    <automated>npm test i18n/translations/en.test.ts -- --run</automated>
  </verify>
  <done>en.ts contains all strings from 9 components, all tests pass, Translation type exported</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Vietnamese translation file matching EN structure</name>
  <files>i18n/translations/vi.ts, i18n/translations/vi.test.ts</files>
  <behavior>
    - Test: vi imports Translation type from en.ts
    - Test: vi object satisfies Translation type (TypeScript enforces structure match)
    - Test: vi has same keys as en (structure validation)
    - Test: Vietnamese content preserved from existing components
  </behavior>
  <action>
Create i18n/translations/vi.test.ts first (RED):
```typescript
import { describe, it, expect } from 'vitest';
import { vi } from './vi';
import { en, Translation } from './en';

describe('Vietnamese translations', () => {
  it('imports and satisfies Translation type', () => {
    const typed: Translation = vi;
    expect(typed).toBeDefined();
  });

  it('has same structure as English', () => {
    const getKeys = (obj: Record<string, any>, prefix = ''): string[] => {
      return Object.entries(obj).flatMap(([key, value]) => {
        const path = prefix ? `${prefix}.${key}` : key;
        if (typeof value === 'object' && value !== null) {
          return getKeys(value, path);
        }
        return [path];
      });
    };

    const enKeys = getKeys(en).sort();
    const viKeys = getKeys(vi).sort();
    expect(viKeys).toEqual(enKeys);
  });

  it('preserves existing Vietnamese content', () => {
    // Spot-check actual Vietnamese strings from existing components
    expect(vi.navbar.overview).toBe('Tong quan');
    expect(vi.hero.titleLine1).toBe('Ky Nguyen');
    expect(vi.productTiers.heading).toBe('Cac Goi Giai Phap');
    expect(vi.footer.brand).toBe('AI-Local Hub');
  });
});
```

Then create i18n/translations/vi.ts (GREEN):

```typescript
import { Translation } from './en';

export const vi: Translation = {
  // Copy exact structure from en.ts
  // Fill in Vietnamese content from existing components
  // ...
};
```

IMPORTANT: Extract Vietnamese text exactly as it appears in existing components. Do NOT translate - preserve the existing Vietnamese content character-for-character. Reference the current component files for exact text.

Use proper Vietnamese characters with diacritics (e.g., "Tong quan" should be "Tổng quan" if that's what's in the component).
  </action>
  <verify>
    <automated>npm test i18n/translations/vi.test.ts -- --run && npm run build</automated>
  </verify>
  <done>vi.ts matches EN structure exactly, TypeScript build passes (type enforcement), all Vietnamese content preserved from existing components</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `npm test -- --run` passes all translation tests
2. `npm run build` succeeds (TypeScript validates EN/VI structure match)
3. Both en.ts and vi.ts export complete translation objects
4. Translation type is properly exported from en.ts
5. vi.ts imports and satisfies Translation type
</verification>

<success_criteria>
- Test infrastructure operational: `npm test -- --run` executes without errors
- English translations complete: All strings from 9 components + App.tsx in en.ts
- Vietnamese translations complete: Exact structure match, existing content preserved
- Type safety enforced: TypeScript compiler validates structure match between EN and VI
- All tests pass: Translation structure tests verify completeness
</success_criteria>

<output>
After completion, create `.planning/phases/01-i18n-foundation/01-01-SUMMARY.md`
</output>

---
phase: 04-svg-chip-diagram
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: []
autonomous: false
requirements: [CHIP-01, CHIP-04]

must_haves:
  truths:
    - "Chip diagram blocks are visually distinct with different colors for CPU, GPU, Neural Engine, and Memory"
    - "Diagram is readable on 320px mobile width without horizontal scrolling or text overlap"
    - "Tab buttons are clickable and visually indicate the active variant"
  artifacts: []
  key_links: []
---

<objective>
Visual verification of the ChipDiagram component on mobile and desktop viewports.

Purpose: Automated tests verify structure and data but cannot confirm visual readability, color contrast, and layout at real screen sizes.
Output: User confirmation that the diagram looks correct.
</objective>

<execution_context>
@/home/dev/.claude/get-shit-done/workflows/execute-plan.md
@/home/dev/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-svg-chip-diagram/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create temporary preview page for ChipDiagram</name>
  <files>components/ChipDiagram.tsx</files>
  <action>
Temporarily add the ChipDiagram component to the existing page so it is visible in the browser. Find the main App component or page entry point and add `<ChipDiagram />` somewhere visible (e.g., below the Hero section or at the top of the page). This is temporary -- Phase 5 will do the real integration.

Start the dev server with `npx vite --host 0.0.0.0` if not already running.
  </action>
  <verify>
    <automated>curl -s http://localhost:5173 | grep -c "ChipDiagram\|chip-diagram\|M4 chip" || echo "Dev server check"</automated>
  </verify>
  <done>ChipDiagram is visible in the browser at localhost:5173.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of chip diagram</name>
  <action>User visually verifies the chip diagram renders correctly on mobile and desktop viewports.</action>
  <what-built>SVG chip diagram component with M4 variant tabs, animated spec numbers, and responsive layout</what-built>
  <how-to-verify>
    1. Open http://localhost:5173 in your browser
    2. Scroll to the ChipDiagram component
    3. Verify 4 distinct colored blocks are visible (CPU blue, GPU purple, Neural Engine green, Memory cyan)
    4. Verify spec numbers animate when scrolling into view
    5. Click "M4 Pro" and "M4 Max" tabs -- verify specs update (GPU: 10 -> 20 -> 40)
    6. Open Chrome DevTools -> Toggle device toolbar -> select 320px width
    7. Verify diagram scales down without horizontal scrolling and text remains readable
    8. Verify tab buttons are visually distinct (active tab highlighted)
  </how-to-verify>
  <verify>User confirms visual correctness</verify>
  <done>User has approved the visual appearance of the chip diagram on both mobile and desktop.</done>
  <resume-signal>Type "approved" if the diagram looks correct, or describe any visual issues to fix</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Remove temporary preview integration</name>
  <files>components/ChipDiagram.tsx</files>
  <action>
Remove the temporary ChipDiagram placement added in Task 1. Revert the App/page component to its original state. The ChipDiagram will be properly integrated in Phase 5.

Verify the full test suite still passes after reverting.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Temporary preview removed. App component restored to original state. Full test suite passes.</done>
</task>

</tasks>

<verification>
User has visually confirmed the diagram renders correctly on both mobile and desktop viewports.
</verification>

<success_criteria>
- User approved the visual appearance of the chip diagram
- Temporary preview code removed
- No regressions in test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-svg-chip-diagram/04-02-SUMMARY.md`
</output>
